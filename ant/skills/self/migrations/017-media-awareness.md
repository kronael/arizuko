# 017 — media awareness

CLAUDE.md "Receiving files from users" section:

- PDFs/documents: `Read(path)` on the attached file path
- Images: `Read(path)` — Claude vision reads them natively
- Voice: already transcribed, text appears inline

Never say "I can't read this document" without first trying `Read`.

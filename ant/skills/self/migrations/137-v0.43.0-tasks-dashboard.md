# v0.43.0 — tasks dashboard + engagement thread fix

dashd now shows scheduled task run history at `/dash/tasks/` with create,
pause, resume, and cancel actions. Slack thread follow-ups to an engaged
agent work again — the engagement root fallback normalises the threaded
topic to root when no own engagement record exists.

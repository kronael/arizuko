# v0.56.1 — skill sync from kronael/tools

New skills available: `/fin`, `/explore`, `/eye-13yo`, `/fable`, `/opus`,
`/humanize`, `/writing`, `/diagrams`, `/review`, `/py`, `/sh`, `/rs`,
`/ts`, `/tsx`, `/scavenge`, `/merge`, `/sub`, `/gh-comment`, `/create`
(router for art/video/web artifacts), `/create-eval`.

Stale individual `create-*` skills replaced by the `/create` router.

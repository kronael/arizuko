# v0.33.19 — inbound reactions documented

`ant/CLAUDE.md` now spells out the inbound-reaction shape: emoji
reactions arrive as small synthetic messages with `verb="like"` (or
`"dislike"`), the emoji as body, and `<reply-to>` pointing at the
message that was reacted to. Treat as acknowledgement of the parent,
not a new turn.

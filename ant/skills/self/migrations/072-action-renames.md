# 072 — action name polish

`delete_post` → `delete` (tool + HTTP endpoint + Socializer method).
`score_down` → `dislike` (future downvote verb, for naming symmetry
with `like`). Consolidates action-name polish.

# Migration 144 — v0.45.5

Bot messages now expose a `platform_id=` attribute in the message XML.
This is the platform-native message id (Slack TS, Telegram msg_id, etc.).

Use `platform_id=` as `targetId` for `like` and `delete` — no need to
call `inspect_messages` and look up `reply_to_id` anymore.

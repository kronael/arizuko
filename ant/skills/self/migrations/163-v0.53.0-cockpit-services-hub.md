# Migration 163 — v0.53.0 — cockpit services hub and federated dashboards

Operator dashboard gains:

- `/dash/services/` — live health grid for all 8 core daemons; `classifyHealth` now
  distinguishes DNS failure (unknown) from connection-refused/timeout (err)
- `/dash/onbod/` federated dashboard — onboarding queue with approve/deny/reprompt
- `/dash/timed/` federated dashboard — task table + loop health via routd `/v1/tasks`
- Portal alert banner — errored chats and failed tasks promoted to a visible warn banner
- `make play` / `make integration` / `make test-all` Makefile targets

# Migration 143 — v0.45.4

CLAUDE.md: fix tool names throughout — `send_reply`→`reply`, `send_message`→`send`.
Add Slack delete/like workflow: use `inspect_messages` → bot row → `reply_to_id` as Slack TS.
Add guidance on Slack `like` errors (scope issue, don't retry).

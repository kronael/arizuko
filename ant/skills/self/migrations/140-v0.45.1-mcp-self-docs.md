# v0.45.1 — mcp.md completeness + /me/ user portal

mcp.md was missing 19 tools and had 3 phantom entries. Fixed:

**Added**: engage, disengage, set_observe_window, observe_group,
unobserve_group, set_group_open, fork_topic, issue_chat_link,
issue_webhook, list_tokens, revoke_token, invite_create,
set_web_route, del_web_route, list_web_routes, get_work, set_work,
pane_set_prompts, pane_set_title, inspect_identity, list_acl,
log_external_cost.

**Removed phantoms**: get_routes (is list_routes), get_grants,
set_grants (not registered).

**web-routing.md + CLAUDE.md**: added /me/* row (user portal —
folder tree, chat list, thread view). Login hint now includes
`$WEB_HOST/me/` for user portal access questions.

# 014 — CLAUDE.md: session continuity + fact staleness

CLAUDE.md gains two behaviors: check diary/logs for past session
context; auto-refresh facts whose `verified_at` is stale.

```bash
cp /workspace/self/container/CLAUDE.md ~/.claude/CLAUDE.md
```

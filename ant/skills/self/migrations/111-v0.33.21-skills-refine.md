# v0.33.21 — skills refine pass

Skills trimmed of redundancy and made consistent. No capability change —
same instructions, less prose. Net ~70 fewer lines of loaded skill
context.

- `mcp/` — dropped duplicate Python and Go examples; same `mcpc` pattern
  in any language, bash example carries it
- `compact-memories/` — collapsed explanatory paragraphs into procedural
  one-liners (Gather, Compress); same rules, fewer words
- `acquire/` — dropped "Useful search services" table (general web
  reference, not load-bearing for the skill's purpose)
- `wisdom/` — consolidated body rules and anti-patterns; restored
  transient-info rule and debugging block
- `dispatch/` — added missing `when_to_use` frontmatter
- `hello/`, `hub/` — added missing `user-invocable: true` frontmatter
- `hub/` — wrapped long frontmatter description into folded YAML

# v0.55.0 — hardening + refine

dashd CSRF/XSS fixes, deliverTurn threading, timed NULL next_run fix,
chanlib typing fix, stale inbound health fix, jid_prefix_mismatch fix,
teled cross-chat message ID collision fix, 3-round dead-code sweep.

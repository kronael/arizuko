# v0.57.1 — skill sync from kronael/tools

Refreshed the vendored skills to their latest versions. No new
top-level skills; the changes are inside existing ones:

- `/review` now splits give vs take — produce findings, or apply them.
- `/software` adds a code baseline + strict-typing guide.
- `/create` adds a code-presentation (slides) mode.
- `/py`, `/rs`, `/ts`, `/fin` expanded.

Nothing to do — the refreshed skills are already seeded.

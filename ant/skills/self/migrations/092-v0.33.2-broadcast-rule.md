## v0.33.2 — release-marker + new convention

No skill changes. New rule: every release ships a migration file —
including docs-only — so the broadcast fires on every CHANGELOG
version through the existing auto-migrate path. Single trigger.

Carries v0.33.1's docs polish (public `/pub/changelog/` page, concept-
page drift fixes) into the chat broadcast — it didn't go out at the
time because v0.33.1 had no migration to bump `MIGRATION_VERSION`.

Spec: `specs/4/P-personas.md ## Versioning`.

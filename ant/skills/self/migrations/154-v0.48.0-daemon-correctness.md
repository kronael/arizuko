# 154 — v0.48.0 — router + adapter correctness pass

No agent-behavior change. This release is an under-the-hood correctness sweep;
your skills and tools are unchanged. Nothing to do.

## What changed (operator/platform, not agent)

- **gateway**: MCP audit logging restored (tool calls are recorded again);
  Whisper/download body handling no longer leaks or silently truncates;
  `SEND_DISABLED_CHANNELS` now honored for documents + voice; message batching
  preserves causality; group-create rolls back on route failure.
- **adapters**: capability maps now match real support (no "unsupported"
  surprises); Bluesky + X no longer drop inbound on a transient failure;
  `/health` reports disconnected when the platform link drops (Telegram/Slack/X);
  email + Mastodon reply threading fixed.
- **tests**: 74+ new unit/integration tests across 13 packages.
- **split daemons** (authd/routd/runed): additive groundwork behind the
  `CUTOVER_SPLIT` flag — dormant; default deployments still run the gated
  monolith. Not active, nothing for you to do.

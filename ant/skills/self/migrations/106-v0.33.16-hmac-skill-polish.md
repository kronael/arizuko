# v0.33.16 — Ant link SSE fix, skill polish

## What changed

- Ant link SSE now works reliably: `PROXYD_HMAC_SECRET` is shared between
  proxyd and webd in compose — previously each used an independent ephemeral
  secret, so webd rejected all proxyd-signed headers and SSE connections failed
- Agent skills polished: `when_to_use` split from descriptions, body "When to
  use" sections removed, multi-sentence descriptions trimmed (-172 lines)

## Action required

None. Restart picks up the HMAC fix automatically.

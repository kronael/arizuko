# 097 — v0.33.7 docs sweep

Stub migration. No skill changes; this file exists to fire the
auto-migrate broadcast for v0.33.7 per the every-release-ships-a-migration
rule (CLAUDE.md ## Shipping changes).

## What changed in the source

- `ROUTING.md` — platform-JID examples now show typed-JID forms
  (`telegram:user/<id>`, `discord:dm/<channel>`, `reddit:comment/<id>`,
  `mastodon:account/<id>`) instead of pre-v0.33.0 untyped strings.
  Route example uses `telegram:group/12345`; sender expansion example
  uses `discord:user/<id>`.
- `GRANTS.md` — `groups` table column list reflects 0041 dropping
  `state` / `spawn_ttl_days` / `archive_closed_days`.
- `ARCHITECTURE.md` — `Group` Key Type description updated;
  oracle skill + ttsd reclassified from "planned" to shipped
  integrations.
- `CLAUDE.md` (root) — same reclassification in core-vs-integrations.
- `EXTENDING.md` — verb support matrix gained the `send_voice` row
  (discd / teled / whapd native; twitd hint-only; rest unsupported).
- `chanlib/README.md` — `NewAdapterMux` route list adds `/send-voice`;
  notes `NoVoiceSender` and `NoFileSender` mixins.
- `ipc/README.md` — tool surface adds `send_voice`, `inject_message`;
  documents `submit_turn` JSON-RPC for per-turn output.
- `SECURITY.md` — boundaries table adds slink-MCP token-as-auth row
  (`POST /slink/<token>/mcp`).
- `crackbox/README.md` — fixed `specscs` typo (5 occurrences) →
  `specs`.

No agent-side action required.

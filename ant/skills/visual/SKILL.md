---
name: visual
description: >
  /visual — launch @visual agent for UI and styling improvements. USE
  for "/visual", "fix this layout", "tweak the styling", "make the SVG
  look right", .css / .svg / UI page tweaks via headful browser
  inspection. NOT for general code (use improve).
user-invocable: true
---

Launch the @visual agent (Task tool, subagent_type: visual) to refine visual elements through render-inspect-adjust.

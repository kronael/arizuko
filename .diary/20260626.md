---
summary: |
  Session: credential model fully shipped (spec 42 complete).
  - whapd re-pair pending (krons, +420735544891)
  - marinade emaid: needs Gmail App Password
  - codex auth expired — needs ! codex login
  - [resolved 2026-06-26: shape 3 REST descriptor shipped (a528fbe1)]
  - [resolved 2026-06-26: grant-gated tools/list shipped (90a28180)]
---

## 00:30

specs/5 open items: 32-tenant-self-service Phase C (BYOA), 37-agent-capability-eval, 41-ext-mcp. BYOA design: per-user encrypted secret table keyed by JID, resolution chain user→folder→operator, injected as container env at runed spawn. arizuko twist: "users" are platform JIDs (telegram:user/...) not login accounts — key registration UX must go through chat command or web link tied to JID, not a login form. Pending user direction on UX path.

## 10:35

atlas Slack thread-reply regression root cause: commit 2b2e6062 over-suppressed — it zeroed threadID (existing thread context) in addition to threadRoot (new thread creation). For in-thread triggers, reply went to main channel → person in thread saw nothing. Fix: 89fbe3cf (routd/turns.go: remove threadID suppression; thread_replies=false now only suppresses threadRoot). TestReplyThreadPrefOffSuppressesThreadID → renamed + inverted. Deployed marinade.

## 10:50

BYOA user-secrets feature: JSON API at /dash/me/secrets already exists but broken (dashd writes plaintext, no HTML page, no spawn-time injection). Opus sub launched to implement comprehensively: fix encryption via routd /v1/secrets API, add HTML page, FolderSecretsResolvedForUser merge, runed spawn injection.

## 11:20

BYOA shipped (24d0b545). 7 changes: store.FolderSecretsResolvedForUser (user>folder chain), routd sibling_db wrapper, dispatch populates RunRequest.Secrets, runed/docker injects as container env, dashd/me_secrets.go encryption fixed (PutSecretRow not raw SQL), HTML page at /dash/me/secrets, profile link. Worktree reconciled via git apply --3way (dashd/main.go conflict: merged dbRuned+runedURL from main + secretKeyring from BYOA). Also: thread_replies CLI arizuko group <inst> thread-replies <folder> <on|off|default> (7e10c71c). Images building; deploying all instances.

## 11:40

Discord thread promotion fix (68b24a84): promotion gate `m.ReplyTo != ""` wrapped ALL of threadHasBotMessage, blocking it for Discord (Discord only sets ReplyTo on explicit reply-arrow use; plain thread messages have ReplyTo=""). Slack sets replyTo=thread_ts for every in-thread message so it was masked. Fix: gate replyTargetIsBot on ReplyTo, let threadHasBotMessage run independently (already guards topic=="" → false). Deployed sloth+marinade.

## 11:55

groups config JSON column (eb968c20, opus sub): migrated model/thread_replies/observe_window_*/open from individual columns into `config TEXT` JSON on groups table. Two migration files: routd/migrations/0014 (routd.db) + store/migrations/0073 (messages.db). All store functions switched to json_extract/json_set/json_remove. Migration tests seed old-schema rows and verify fold. Tests green. Deploying all instances.

## 12:30

Threading regression investigation post-v0.56.0: NOT a regression. mcp-* initial replies have topic="" by design (turn_id=thread_ts is the lookup key — TestThreadHasBotMessage_MatchesByTurnID). out-* messages with topic="" were Slack API send failures during the restart window (deliverRow error path → topic stays ""). Subsequent turns carry topic=1782468968.124789, confirming thread was created.

## 12:36

Skill sync from kronael/tools (42e00f83, 9769d1ec): added 22 skills to ant/skills — fin, explore, eye-13yo, fable, opus, humanize, writing, diagrams, review, py, sh, rs, ts, tsx, scavenge, merge, sub, gh-comment, create router, create-eval. Removed 14 stale create-* dirs (consolidated into create/ router). Updated scripts/sync-ant-skills.sh VENDORED list. Migration 169, agent image rebuilt.

## 14:20

BYOA test coverage: added TestMeSecrets_CrossUserIsolation (Bob's GET can't see Alice's secrets — SQL scope_id=?) and TestDispatchTimedTriggerNoUserSecrets (timed-isolated:* hits service:routd path, no user BYOA key in RunRequest.Secrets). Commit 161ea01a. All tests green.

## 14:35

ext-mcp (specs/5/41) review: shape 2 (MCP subprocess connector) shipped — GitHub wirable via connectors.toml today. Shape 3 (REST descriptor, the "generic CURL") not shipped — covers Buildkite + any API without an MCP server. ANTHROPIC_API_KEY is a special case: operator anchor injected into container env at spawn, not from the secrets table — separate trust boundary from BYOA/folder secrets. User direction pending on build priority (shape 3 vs GitHub subprocess first).

## 15:20

Credential model redesign: user clarified three distinct types — env-profile (Claude/Anthropic keys, user-only, spawn-time env), capability credentials (GITHUB_TOKEN etc, user-owned grant-gated, call-time broker target), infra/operator (channel tokens, .env only). scope_kind={folder,user} was wrong abstraction. Wrote specs/5/42-credentials.md consolidating 41 §secrets, 32 §Phase-C, 7/E §Anthropic-keys. Key findings: ConnectorSecrets bug (folder scope only, BYOA key silently invisible to MCP subprocess), chats.kind gate dropped, callerSub≠ACL principal distinction documented.

## 15:45

ConnectorSecrets fix (0d244973): sibling_db.go:ConnectorSecrets now takes callerSub param + calls FolderSecretsForUser. routd/mcp.go:buildStoreFns captures t.trigger in closure — no ipc.go type change needed. TestConnectorSecrets_UserBYOAWins added: alice wins over folder, bob falls back, empty caller gets folder. All tests green.

## 16:15

spec 42 credential model fully shipped. 6 commits: (1) store-layer enforcement — EnvProfileKeys set, validateScope rejects folder-scope for ANTHROPIC_API_KEY/CLAUDE_CODE_OAUTH_TOKEN/OPENAI_API_KEY/CODEX_API_KEY; (2) /dash/me/env new dashd endpoint for model API keys — HTML + JSON CRUD, operator fallback shown read-only; (3) /dash/me/secrets now rejects env-profile keys with pointer to /dash/me/env; (4) grant-gated tools/list — Authorize check before connector registration in ipc.go:1019; (5) shape 3 REST descriptor (a528fbe1) — ipc/extcall.go dispatcher (5 auth methods: bearer/apikey-header/apikey-query/basic/json-body, path param substitution, secret scrub), routd/ext.go TOML loader, built-in providers cloudflare/porkbun/gandi/namecheap; (6) test fixes — tests that seeded env-profile keys at folder scope updated to use GITHUB_TOKEN. Full test suite green.

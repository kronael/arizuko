---
summary: |
  arizuko multitenant agent router. Main user: onvos.
  - v0.33.15 shipped: web_routes MCP, auth-gate default, ant link rebrand, UID fix, voice
  - ant link (slink) SSE auth may need PROXYD_HMAC_SECRET consistency check on krons
  - teled stale on krons+sloth (no recent Telegram traffic, not a regression)
  - howto/welcome content after invite still missing (next UX gap)
---

## 06:30

Slink (ant link) mobile fixes: `height:100dvh`, `safe-area-inset-bottom` on footer,
`font-size:16px` on textarea (iOS zoom prevention), `min-height:44px` on send button.
SSE reconnect: `onerror → setTimeout(connect, 3000)`. Typing indicator `…` shown after
send, removed on assistant reply. Branding: "web chat" → "ant link".

## 06:35

New skill: `ant/skills/slink/SKILL.md` — teaches ant its own ant link URL
(`echo "https://$WEB_HOST/slink/$SLINK_TOKEN"`), sharing pattern, round-handle API,
MCP surface. Uses "ant link" terminology throughout.

## 06:45

Platform/rebrand discussion: user wants groups called "ants" (agents), slinks called
"ant links". ant/ CLI to eventually be renamed "box" but left as-is for now. Parked
larger platform design (agent tree, agent import/export) — user pivoted to concrete fixes.

## 06:50

Onbod invite flow fixed: direct invite (`target=coach`) now lands on group's ant link
(`/slink/<token>`) instead of `/onboard` dashboard. Subworld invite (`target=coach/`)
now seeds `slink_token` + `product` in the INSERT so the new group has an ant link
immediately, then redirects to it after world creation. Test schema updated. Deployed sloth.

## 08:30

UID mismatch root cause: CLI runs as onvos (UID 1001), container node user = UID 1000.
Fix: `seedGroupDir` now calls `chownR(groupDir, 1000, 1000)` after seeding. Also added
`ant/CLAUDE.md` storage table: `/home/node/` is persistent (volume-mounted), `/tmp/` is
lost on container exit, `/home/node/tmp/` is scratch-but-survives.

## 08:45

Proxyd routing bug diagnosed: catch-all (main.go:425) does 302 `/pub`+path for ALL unknown
paths — no auth, no existence check. `/priv/` is not a real route; silently lands on
`/pub/priv/` (public). `/x/` is auth-gated but routes to webdProxy not viteProxy.
Options: (1) 404 unknown paths, (2) add `/priv/` as auth-static route. Awaiting user decision.

## 10:00

Proxyd routing resolved: unknown paths now auth-gated by default (requireAuth wraps servePub
transparent rewrite). Authenticated users can access any path under `/pub/`; unauthenticated
browsers redirect to `/auth/login`, JSON/API clients get 401.

## 10:15

Agent-controlled web routing shipped: `web_routes` SQLite table (migration 0045), `MatchWebRoute`
longest-prefix LIKE query, MCP tools `set_web_route` / `del_web_route` / `list_web_routes` in
ipc.go, wired through gateway.go. proxyd queries DB directly per request (no cache). Access
values: `public` | `auth` | `deny` | `redirect`. Spec at `specs/6/6-web-routes.md`.

## 10:30

Refine pass (3 simplify subs): extracted `scanWebRoute` helper, promoted `servePub` to method,
replaced 6-call htmlEscape with `strings.NewReplacer`, reused `worldOf` in `buildMounts`,
collapsed `del_web_route` to single DELETE+rows-affected query (was List+Delete TOCTOU),
extracted `parseBefore` in ipc. All tests passing. 8 commits landed this session.

## 19:30

Second refine pass. Tests were failing: `handleCreateWorld` panicked on nil `cfg.core` — a
simplify agent had removed the `core.LoadConfig()` fallback as "dead code". Restored it.
Integration tests set `DATA_DIR` env; unit tests fall back to cwd. onbod/README.md updated.

## 19:40

v0.33.15 released: tagged, images built (`arizuko:v0.33.15`, `arizuko-ant:v0.33.15`),
migration 105 added, krons restarted. Smoke: marinade healthy, krons+sloth teled `stale`
(no recent Telegram messages — connected, not broken).

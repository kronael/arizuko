---
summary: |
  arizuko multitenant agent router. Main user: onvos.
  - platform vision: groups → "ants" (agents), slinks → "ant links"; ant/ CLI stays as-is
  - slink chat mobile broken + "strengths" issue still unclear — asked user to clarify
  - invite flow fixed: direct invite → ant link, subworld → username picker → ant link
  - howto/welcome content after invite still missing (next UX gap)
  - platform discussion (groups as agents, agent tree) parked — user pivoted to slink fixes
---

## 06:30

Slink (ant link) mobile fixes: `height:100dvh`, `safe-area-inset-bottom` on footer,
`font-size:16px` on textarea (iOS zoom prevention), `min-height:44px` on send button.
SSE reconnect: `onerror → setTimeout(connect, 3000)`. Typing indicator `…` shown after
send, removed on assistant reply. Branding: "web chat" → "ant link".

## 06:35

New skill: `ant/skills/slink/SKILL.md` — teaches ant its own ant link URL
(`echo "https://$WEB_HOST/slink/$SLINK_TOKEN"`), sharing pattern, round-handle API,
MCP surface. Uses "ant link" terminology throughout.

## 06:45

Platform/rebrand discussion: user wants groups called "ants" (agents), slinks called
"ant links". ant/ CLI to eventually be renamed "box" but left as-is for now. Parked
larger platform design (agent tree, agent import/export) — user pivoted to concrete fixes.

## 06:50

Onbod invite flow fixed: direct invite (`target=coach`) now lands on group's ant link
(`/slink/<token>`) instead of `/onboard` dashboard. Subworld invite (`target=coach/`)
now seeds `slink_token` + `product` in the INSERT so the new group has an ant link
immediately, then redirects to it after world creation. Test schema updated. Deployed sloth.

---
summary: |
  Auth + identity design session. Main user: onvos.
  - spec 28: user-centric identity (user→JIDs→groups→routes)
  - spec 29: local CLI for programs to reach groups (draft)
  - proxyd: fixed /pub/* routing, unknown→/pub/ redirect, deployed
  - oauth: GitHub configured for all 3 instances
  - open: implement spec 28 (ACL inversion, slink unification, dashboard)
---

## 15:00

Design session continued: user-centric identity model solidified.
User owns JIDs, has group access, manages routes. Dashboard in onbod
(progressive: create world if new, manage routing if existing).
Rewrote spec 28 as "User Identity & Auth". Added spec 29 (local CLI
for programs to reach groups, open questions only).

## 13:30

Fixed proxyd: /pub/* was routing to webd instead of vite (broke when
webd integrated). Added unknown-path→/pub/ redirect. Configured GitHub
OAuth on all 3 instances. Added slink docs to howto+web skills,
migration 056. Auth design: no-rows=no-access, slink→scoped auth
tokens. Created specs/8/28.

## 10:45

Shipped webd integration: added webdService() to compose generation,
webd binary to Dockerfile, auto-generate slink tokens in PutGroup,
fixed {folder...}/suffix route panic in webd/server.go. Backfilled
slink tokens for all 14 groups. Deployed all 3 instances — webd
registered as web channel, slink POST verified working (200, stored).
Mass onboarding spec written at specs/8/27-mass-onboarding.md.

## 09:00

Design session: mass onboarding + rate limits. Iterated config model
3x — landed on: channel→world in service TOMLs, world limits in .env,
channel map CSV passed to onbod at compose-gen time. Then user reframed
onboarding entirely: username=world, web auth gate (not admin/auto),
multi-platform JIDs per user. Token-based flow: bot sends link → user
authenticates via OAuth → picks username → world created. Bigger scope
than original plan. Rate limiter (per-world spawn limits) unchanged.
Filed .env cleanup issue in bugs.md.

## 07:45

Addressed all eval findings across 3 instances: fixed missing skills
(marinade/atlas), added YAML frontmatter (krons/rhias nemo-*), moved
orphaned skills from double-nested .claude/.claude/ (sloth/main),
deleted rogue SOUL.md + dead songwriting dir (sloth/content), cleared
errored state (krons/happy). Rebuilt arizuko + arizuko-ant images,
deployed all instances. Planned auto-approve onboarding (per-user
groups, 1/day cap) + per-JID spawn rate limiter. Plan awaiting approval.

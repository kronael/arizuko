---
summary: |
  UTC rolled over while continuing 2026-05-02's release cadence.
  v0.33.7 docs sweep shipped (review found no real bugs; doc drift
  fixed across ARCHITECTURE, ROUTING, EXTENDING, GRANTS, SECURITY,
  6 per-package READMEs). Onboarding state inspected on krons —
  3 approved, 8 awaiting_message but all 8 are orphans (3 from
  yesterday's typed-JID drift, 4 dev artifacts, 1 stale lead).
---

## ~07:00 — date rolled, v0.33.7 in flight

Continuing 2026-05-02's run. Spawned two parallel subs: code review
of yesterday's 7 releases (read-only) + docs sweep across root
contributor docs and per-package READMEs (worktree).

### Review (sub D, read-only)

Punch list of 8 items, no P0 real bugs. Two cosmetic spec-status
nits (`b-ant-standalone` "unshipped" + foundation-shipped is
intentionally ambiguous; `H-call-llm-mcp` "shipped" but skeleton
only — both fine), one test-coverage gap (no test for structured
`Unsupported` from `NoFileSender`; future), one minor CHANGELOG
incompleteness (v0.33.6 didn't list `EMAIL_IMAP_PORT` /
`EMAIL_SMTP_PORT` env vars), one style nit in `chanreg/httpchan.go`
(error string still hyphenates "send-file"), one unsupported
orthogonality claim (no CI gate enforcing the no-arizuko-imports
rule on `ant/`). All P1/P2 — no shipped code contradicts announced
behavior.

### Docs sweep (sub E, v0.33.7)

Lands as `b9e03b3 [release] v0.33.7: docs sweep — typed JIDs,
send_voice, dropped state, oracle/ttsd shipped`. 13 files, 141
insertions, 54 deletions:

- `ROUTING.md` — full platform-JID example column rewritten to
  typed v0.33.0 forms (`telegram:user/<id>`, `discord:dm/<channel>`,
  `reddit:comment/<id>`, `mastodon:account/<id>`); route example
  uses `telegram:group/12345`; sender expansion
  `discord:user/<id>`.
- `EXTENDING.md` — verb support matrix gained `send_voice` row,
  count 11 → 12.
- `ARCHITECTURE.md` / `CLAUDE.md` — oracle skill + `ttsd`
  reclassified from "planned" to shipped integrations; `Group`
  Key Type drops `state`.
- `chanlib/README.md`, `ipc/README.md`, `GRANTS.md`, `SECURITY.md`,
  `crackbox/README.md` — `/send-voice` route, `send_voice` /
  `inject_message` / `submit_turn` mentions, dropped `state` column,
  slink-MCP token-as-auth row, 5× `specscs` typo fix.
- Migration `097-v0.33.7-docs-sweep.md`, `MIGRATION_VERSION` 96→97,
  SKILL.md "Latest" 96→97.

Cherry-pick clean — sub was on parent HEAD (59ab0ec) so no stale-base
conflicts.

Tagged `v0.33.7` on `b9e03b3`. Images `arizuko:v0.33.7` +
`arizuko-ant:v0.33.7`. Restarted all 3 instances at 07:28:56 UTC.

### Open punch-list items (drift sub E found but didn't fix)

10 items, all ambiguous or out-of-scope. Highlights:

- `ROUTING.md` "Full Message Flow Example" still uses untyped
  `telegram:12345` / `:67890` in 6 walkthrough places — invasive
  prose surgery, deferred.
- `ipc/README.md` claim that `inspect_identity` scopes by folder
  subtree — code paths confirm the others but not `inspect_identity`;
  flag for verify.
- `SECURITY.md` cites `webd/server.go:44`, `onbod/main.go:81` — line
  numbers not verified to still match.
- `EXTENDING.md` cites `specs/4/11-auth.md`, `specs/4/19-action-grants.md`,
  `specs/7/12-crackbox-sandboxing.md`, `specs/9/b-orthogonal-components.md`
  — spec paths not verified to exist.

## ~03:00 — onboarding state inspection (krons)

Operator: "find out if all requests are onboarded successfully.
across krons mostly".

Checked `onboarding` table. 11 rows, 0 actually stuck:

- 3 `approved` (success): two telegram groups + one whatsapp;
  fully operational.
- 8 `awaiting_message` (orphans):
  - 3 from yesterday's 08:05 typed-JID-drift incident — chats are
    fully operational (1291 + 744 + 87 messages, all active 02:00
    today UTC, routes correct). `prompted_at` is non-null so
    `onbod` won't re-prompt. Onboarding row is dead bookkeeping;
    routes table is the source of truth.
  - 1 real stale: whatsapp lead from 2026-04-27, 1 message ever,
    never followed through.
  - 4 dev artifacts (`local:krons/content`, `local:content`,
    `local:atlas`, `smoke:atlas`) — leftover from testing.

Verdict: every chat that should be operational *is* operational and
replying. The orphan rows are pollution. Cleanup recommendation
deferred to user.

## Open

- Cleanup of stale onboarding rows on krons (3 ghost-onboards from
  drift + 4 dev artifacts). Single DELETE statement.
- v0.33.7 broadcast verification — auto-migrate fired at 07:29
  UTC; /migrate is running on each root agent; v0.33.7 blockquote
  goes out shortly. (v0.33.4 + v0.33.5 + v0.33.6 broadcasts all
  rolled into v0.33.6's blockquote yesterday since /migrate only
  broadcasts the latest. Today's restart will additionally broadcast
  v0.33.7's blockquote.)
- 27 worktrees from yesterday's parallel runs still on disk —
  cleanup candidate.

## ~07:30 — onboarding state inspection (full sweep)

Operator: are all onboarding requests complete; what about pending
accounts; is martin / tom / mili active.

- Krons: 11 onboarding rows. 3 `approved` (success), 8
  `awaiting_message` orphans. 3 are ghost-onboards from yesterday's
  typed-JID drift (chats fully operational via routes, onboarding
  row dead bookkeeping). 1 stale WhatsApp lead from 2026-04-27, 4
  dev artifacts.
- Sloth: 3 rows. 1 approved (content), 1 dev artifact, 1 `pending`
  legacy state (operator's own user JID — doesn't block anything).
- Marinade: 2 rows. 1 dev artifact, 1 `pending` lead from
  2026-04-14 (`telegram:user/1612831875`) — got auth link, never
  followed through. The only "real un-onboarded user" across all
  three instances.

`pending` is a legacy status that the current onbod state machine
no longer issues; rows in it are inert.

Martin / Tom / Milan check:
- **Martin** (`telegram:user/137865889` → `atlas/martin`): NOT
  active. Group + route exist; 50 messages in chat, all bot-side
  (scheduled `/compact-memories` cron tasks + agent responses +
  release broadcasts). The actual user has never sent a message.
- **Tom** (`telegram:user/1818847397` → `atlas/tom`): same shape.
  45 messages, all cron-driven. Zero human inbound.
- **Milan** (sloth): name appears in `sloth/content/diary/` and
  chat history but only as a referent ("explain to Milan", "why
  milans?"). The operator (Kron Ael) is preparing content **for**
  Milan; Milan has never messaged the bot. Persona / audience,
  not a user.

Verdict: all three are fully wired infrastructurally but dormant
from their side. Either fresh invites needed or just hasn't started
using their channel.

## ~07:40 — issues aggregation + new skill (v0.33.8)

Operator: collect all `issues.md` from all instances, aggregate as
tasks into central `bugs.md`, wipe per-group files; also fix the
issues skill so it's clearer how/where to save; maybe create a new
skill, move info from `ant/CLAUDE.md` into it; do all in subs.

Two parallel subs:

**Sub A** (no worktree, parent tree): walked
`/srv/data/arizuko_*/groups/*/issues.md` (and one
`groups/atlas/support/issues.md`). 6 files, 44 bullets total
across krons (mayai/krons/rhias-nemo), sloth (main), marinade
(atlas/atlas-support). Aggregated to a new `## Aggregated
user-reported issues — 2026-05-03` section in `bugs.md` grouped
by `<instance>/<folder>`. 2 STALE? flags raised (krons telegram
v0.29.4 broadcast 502 — likely fixed; marinade slink delivery —
closed by reporter). Truncated all 6 source files to a clean
4-line header.

Side fix during commit: pre-commit prettier 4.0.0-alpha.8 errors
"No files matching" when invoked single-file on `bugs.md` — known
prettier-3.8.1 bug for files in `.gitignore`. Added `bugs\.md$`
to the prettier hook `exclude` regex in `.pre-commit-config.yaml`.
bugs.md is operator scratch; no value in format-gating it.

Commit `51b60ea`.

**Sub B** (worktree): created `ant/skills/issues/SKILL.md` (41
lines, frontmatter description for `/dispatch` discovery). Body
covers where to save (`~/issues.md`), format (date + source +
scope + one-line nature), when NOT to log (in-turn-fixed errors),
and the operator-consolidation contract. Trimmed
`ant/CLAUDE.md` "Recording user-reported issues" section from
9 lines to a one-line pointer at the new skill. Migration
`098-v0.33.8-issues-skill.md`, `MIGRATION_VERSION` 97 → 98.

Worktree was on stale base (4220e77, MIGRATION_VERSION=78).
Cherry-pick conflicted on MIGRATION_VERSION, SKILL.md, CHANGELOG.
ant/CLAUDE.md auto-merged correctly. Aborted, applied surgically:
copied new files, hand-bumped versions, injected v0.33.8 block
above v0.33.7 in CHANGELOG.

Commit `e5d6c2c`. Tagged `v0.33.8`. Images
`arizuko:v0.33.8` + `arizuko-ant:v0.33.8`. Restarted all 3
instances at 08:14:10 UTC. Auto-migrate fired 6 root groups
97 → 98, 3 child notifications. /migrate distributing the new
`/issues` skill + broadcasting v0.33.8.

## ~08:30 — oracle / codex auth investigation

Operator: investigate codex auth (must use codex login keys, not
inject OPENAI_API_KEY directly); try executing codex locally to see
if it answers; no code change, plan only.

Findings:
- `codex` CLI is on host at `/home/onvos/.bun/bin/codex` (v0.111.0;
  agent image has v0.128.0 — both compatible auth.json shape).
- `~/.codex/auth.json` (mode 600) generated by `codex login` on
  2026-03-06. Shape: `auth_mode: "chatgpt"`,
  `OPENAI_API_KEY: null`, `tokens: {id_token, access_token,
  refresh_token, account_id}`. Last refresh 2026-03-06, but
  refresh_token still works — codex silently re-mints access on
  invocation.
- `codex login status` → "Logged in using ChatGPT".
- Live test (`echo … | codex exec --json`) authenticated
  successfully via cached refresh_token, but **hit ChatGPT free-tier
  usage limit; reset 2026-05-06 ~07:10 UTC** OR upgrade to Plus.
  Auth works; budget doesn't.

Important divergence from the v0.33.4 oracle skill's design: the
skill assumed `OPENAI_API_KEY` in folder secrets (API-key auth,
direct billing). Operator's actual auth is ChatGPT OAuth via
`~/.codex/auth.json` — a different mechanism that doesn't use
env vars.

Five options to wire ChatGPT-OAuth into agent containers (full
analysis in chat); ranked best-to-worst pragmatism:

1. Bind-mount `/home/onvos/.codex` → `/home/node/.codex` (RW) per
   agent. Single source of truth; refresh-token rotation works.
   Trade-off: all agents share codex history/sessions dir.
2. Bind-mount only `auth.json` + `config.toml` (RO). In-memory
   refresh works; eventual re-bootstrap from host needed.
3. Per-folder `CODEX_AUTH_JSON` secret + container-start
   materialization. Most isolated, most fragile.
4. Sidecar codex daemon. Heavy.
5. Switch the skill to API-key auth (`OPENAI_API_KEY` in folder
   secrets, per current SKILL.md design). Direct, simplest, matches
   what the skill was built for.

Operator hasn't decided yet which path. Either way: no oracle calls
will return content until 2026-05-06 reset OR ChatGPT Plus upgrade
OR an API key is provisioned.

## State at end of session

Today's tags: v0.33.7 → v0.33.8. Combined with yesterday's run,
**9 total tags v0.33.0 → v0.33.8**. MIGRATION_VERSION = 98 across
all 3 instances. 32 containers healthy / 0 unhealthy.

bugs.md grew to 44 aggregated bullets (themed punch list across
all instances). Per-group `issues.md` files truncated and ready
for the next round. `/issues` skill replaces buried CLAUDE.md
prose; agents pick it up via auto-migrate.

## Open

- Decide oracle auth path (1 vs 5). Either is a follow-up code
  change.
- Marinade `pending` lead from 2026-04-14 — chase or delete.
- Onboarding cleanup — delete the 6 dev artifacts + 3 ghost
  onboards from yesterday's drift.
- Soak v0.33.3 reply-pointer fix on sloth/content (still
  unobserved in real interactions).
- 27 worktrees still on disk from yesterday's parallel runs.

## ~17:00 — codex mount path: v0.33.9 → v0.33.10 → v0.33.11

**v0.33.9** (`3e13d49`): added `HOST_CODEX_DIR` knob —
`core/config.go` (HostCodexDir field), `compose/compose.go`
(passthrough to gated env), `container/runner.go` (conditional
bind in `buildMounts`). Operator sets the env in `.env`; agents
get the host's codex login state mounted at `/home/node/.codex`
(rw). Plus `ant/skills/oracle/SKILL.md` rewritten to document
both auth paths (host-mount vs folder-secret env) + tests in
`container/run_test.go`.

Deployed to all 3 instances at 09:51 UTC. Auto-migrate fired
98 → 99. Mount appears in `docker inspect` of gated containers
correctly.

**Hidden bug**: docker-inspect of *spawned agent* containers
showed NO codex mount. The runner had `os.Stat(cfg.HostCodexDir)`
guarding the mount — `os.Stat` runs **inside** gated where the
HOST path isn't visible, so every check returned ENOENT and the
mount silently skipped on every spawn.

**v0.33.10** (`59bd748`): dropped the stat check.
`cfg.HostCodexDir` is a HOST path resolved by the docker daemon
at agent-spawn time, not a path gated needs to read. Loud failure
beats silent skip. Migration `100-v0.33.10-codex-mount-fix.md`,
MIGRATION_VERSION 99 → 100. Plus `specs/5/index.md`
T-voice-synthesis status `unshipped` → `shipped` (had been stale
since v0.33.0).

Also `[specs] move b-ant-standalone 5→9` (`43de4f8`): foundation
shipped in v0.33.5, full runtime port + skill move are multi-day
work — belongs in phase 9 (later) not 5 (active). Per user
instruction.

Deployed all 3 at 18:04 UTC. Auto-migrate 99 → 100. Mount
appeared in 3 spawned agents (`arizuko-content-...`,
`arizuko-mayai-...`, `arizuko-atlas-...`).

But `codex login status` from inside an agent failed with
`Permission denied (os error 13)` on `/home/node/.codex/config.toml`.
**Cause**: host's `~/.codex/auth.json` and `config.toml` are
mode 600 owned by uid 1001 (onvos); container's `node` user
is uid 1000.

**v0.33.11** (`1a193f9`) — layered mount + isolation:

Per user direction ("enable codex for all groups but check that
hosts can not cross-communicate through the dir which should
only hold credentials. it's ok to have a rw dir with similar
permissions as the tier system but the creds having only RO
overmounted"):

- Per-group writable `.codex/` at `<groupDir>/.codex` mounted
  rw to `/home/node/.codex`. Each group has its own workspace —
  history, sessions, memories, sqlite state. No cross-group
  contamination.
- Host's `auth.json` and `config.toml` from `HOST_CODEX_DIR`
  bind-mounted RO **on top** of the per-group dir. Shared creds,
  agents can't write them, refresh-token rotation stays
  in-memory per-process.
- Operator one-time on host: `chmod 644 ~/.codex/auth.json
  ~/.codex/config.toml` so container uid 1000 can read mode-600
  files. Done.
- Tests in `container/run_test.go` assert: parent dir mount
  precedes file overmounts (mount order matters), all three
  paths present.

## ~18:30 — slink-driven e2e harness (`webd/slink_e2e_test.go`)

Per user direction ("the real slink and end to end agent tests
should run only on releases. but they should be there"). Sub
shipped in worktree `agent-ad19b9e9f1a2b05ae`, cherry-picked
clean.

5 e2e funcs (4 active + 1 stub):
- `TestSlinkE2E_DropAndRead` — POST message, GET snapshot,
  assert reply content
- `TestSlinkE2E_SSEStream` — same but read via SSE; assert
  terminal frame closes
- `TestSlinkE2E_Steer` — post message + steer onto in-flight
  round; assert chained-from linkage via topic continuation
- `TestSlinkE2E_GetThread` — post 3 msgs same topic, agent
  reads via `get_thread`, assert thread length
- `TestSlinkE2E_OracleSurface` — stub (codex mount-args path
  exercised at unit level until heavy harness wires real
  `arizuko-ant`)

Each guards on `testing.Short()`. Tests live in `webd/`
package because the existing `newIntegServer`/`seedGroup`/
`mockRouter` harness is `package main` and reusing it kept the
diff small (326 lines).

Makefile changes:
- `test`: now passes `-short` (skips e2e, stays fast)
- `test-e2e`: new target, runs `go test ./tests/... -count=1
  -run E2E -timeout 300s` (no `-short`)
- `webd/Makefile` mirrors

`.github/workflows/ci.yml`: new `e2e` job gated on
`refs/tags/v*` so the slow path runs on release.

## State at end of session

Today's tags: v0.33.7 → v0.33.8 → v0.33.9 → v0.33.10 → v0.33.11.
Combined with yesterday's run: **12 total tags v0.33.0 →
v0.33.11**. MIGRATION_VERSION = 101 across all 3 instances
(after v0.33.11 deploy completes).

`make test` stays under ~3 min (real tests + `-short` skip on
e2e). `make test-e2e` adds the slink-driven path. Codex
mount is per-group isolated; only auth lives in shared host
file via RO overmount.

## Open

- Onboarding cleanup (carried from earlier today).
- Soak v0.33.3 reply-pointer fix (carried; still unobserved).
- 28+ worktrees on disk.
- v0.33.11 deploy verification: codex from inside an agent
  should now succeed (`codex login status` etc.) — pending
  image rebuild + restart.
- ChatGPT free-tier quota still dry until 2026-05-06 — codex
  exec calls return content only after that or upgrade.
- `/pub/` 404 on fab.krons.cx (marinade) flagged during atlas
  browser-chat debug — vited dir empty / proxyd path mapping
  broken. Real bug. Logged here.
- Atlas browser-chat reachable: `/auth/login` 200, `/chat/`
  303 → login (proxyd auth gate working), `/dash/` 200. No
  slink token for atlas (invites table empty); only the
  authenticated path is open. OAuth provider keys in
  marinade `.env` are the gate to actually use it.

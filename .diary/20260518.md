---
summary: |
  arizuko multitenant Claude agent router. Main user: onvos.
  - engagement fully corrected: overrides ALL routes + reactions inherit topic
  - Atlas persona merged (SOUL+PERSONA → one s'more character); disengage instruction added
  - SOUL.md → PERSONA.md rename swept across 48 files
  - web docs rsync to krons pending (permission denied today)
---

## 06:28

v0.40.9 shipped. Two features + 4-round codebase refine:

**Engagement** (spec 5/G): `chat_reply_state.engaged_until + engaged_folder` — bot stays listening
in a `(jid, topic)` for 10m after each reply or mention. `resolveOrEngaged` gateway fallback
delivers inbounds without a fresh @mention. `engage`/`disengage` MCP tools with three-arm authz.

**Per-surface output styles** (spec 5/Y): runner derives `<platform>-<surface>` from JID shape;
ships `slack-{dm,channel,thread,pane}`, `telegram-{dm,group}`, `discord-{dm,channel}`, `web.md`.

**Refine** (4 rounds, ~300 LOC removed): dropped dead `botMsgs`/`sentIDs` in discd, unified email
renderer in emaid, extracted `sseHeaders` in webd, removed `pageTop`/`dashNav` from dashd,
dropped `signState` wrapper + vestigial bool param in auth, unified JID parser in chanlib.

Spec IDs: `I-modality-envelope` → `X`, `O-output-styles` → `Y` (base58 compliance).

Migration 133 (0058-engagement-column.sql) ran on krons, marinade, sloth. All healthy.

## 08:35

v0.41.0 deployed to krons, marinade, sloth. Migration 0059 (route_tokens table) applied to all
three DBs. Migration 134 skill broadcast sent by root groups.

**route_tokens** (spec 5/W): `route_tokens` table (sha256-hashed, raw never stored). Two kinds:
`web:<folder>` (chat widget) and `hook:<folder>/<label>` (webhook ingest). `/slink/*` → `/chat/*`
+ `/hook/*`; operator panel `/chat/<folder>` → `/panel/<folder>`. Migration 0059 drops
`groups.slink_token` column.

**API surface**: `/v1/route_tokens` REST + 4 MCP tools (`issue_chat_link`, `issue_webhook`,
`list_tokens`, `revoke_token`). `arizuko token issue|list|revoke` CLI. dashd operator UI at
`/dash/tokens/<folder>/`.

**Atlas/strengths (marinade)**: issued `web:atlas/strengths` chat token
`n7S4TASzDjR9hK8ZTlmG3l5WEMJfs-avM8SSpoxcvD0`. Updated `submit.html` directly (CHAT_TOKEN
constant + /chat/ URLs). Updated `build-submit-page.sh` to read from workspace/chat-token.txt.
Token saved at `/srv/data/arizuko_marinade/groups/atlas/strengths/workspace/chat-token.txt`.

**Nemo/krons**: auto-migrate triggered 3x today; NEMO responded with persona intro instead of
running /migrate (migrate skill is root-only — child group expected behavior). Skills seeded to
v134 via seedSkills on container start. Rhias (root) ran migration successfully at 08:23.

## 12:20

**Engagement bug sweep** — three bugs found and fixed post-v0.41.0 deploy:

1. **Observe-only scope** (gateway.go): engagement override only fired when `rt.Mode == "observe"`. Spec 5/G requires full route override. Fixed: engagement check now runs before the observe branch, overrides any route.

2. **Reactions missing Topic** (api.go + slakd + discd): reactions to threaded messages arrived with `Topic=""`, breaking engagement and observe-override. Fixed: `api.go` inherits Topic from parent via `store.MessageByID` when empty + ReplyTo set. Slack also captures `thread_ts` from event directly.

3. **Child-group migration backstop** (gateway.go): removed block that sent system messages to child groups on version bump — was causing NEMO to self-introduce on every migration.

**SOUL.md → PERSONA.md** sweep: 48 files updated. Auto-migration in runner.go kept for backward compat. Atlas's two conflicting persona files merged into one s'more character: cosmic/caveman primary voice + cite-first grep discipline as method. `disengage()` call instruction added for thread-done detection.

**Spec/docs**: G-engagement spec corrected (was "observe must not override" → "engagement overrides all routes"). ROUTING.md, ARCHITECTURE.md, CHANGELOG.md, routing.html, engagement.html, mcp.html, env.html all updated. Stale `6/G` refs fixed across specs 5/X, 5/F, 6/D.

Deployed to krons, marinade, sloth. All running.

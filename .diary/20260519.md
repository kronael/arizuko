---
summary: |
  arizuko dashd overhaul + platform hardening. Main user: onvos.
  - messages.db content/raw columns still plaintext (CEO blocker — spec 6/E deferred)
  - SSO/SAML spec drafted (6/X) but not shipped — enterprise procurement blocker
  - Slack typing: scopes confirmed (im:write + channels:write); indicator fires on next atlas response
  - secrets broker (worktree a71b211) intentionally not merged — contradicts 4ef4864 deferral
  - whapd_krons: 401-loop (session invalidated) — needs re-pair with --pair flag
---

## 04:10

HOST_APP_DIR made mandatory — removed execDir() fallback that caused recursive skills
nesting on sloth (binary not in app root → wrong path → agent seeded from ~/.claude/).
Fixed onbod TestMain + core config tests.

## 04:20

Tier policy lifted: tier 2 now = tier 1 scoped to own subtree. policy.go, ipc.go,
container/runner.go all updated. Tier 3+ retains old restrictions. Egress wildcard
extended to tier 2. Web mount also extended to tier 2 (shares parent world vhost,
writes to /workspace/web/<groupname>/).

## 04:35

Secrets table AES-256-GCM encryption shipped. Key = SHA-256(AUTH_SECRET). No
migration compat — plaintext rows purged on startup (PurgeUnencryptedSecrets).
Operators re-enter secrets after first encrypted boot.

## 04:45

Spec 6/X (SSO SAML 2.0 + OIDC) written. Layered on existing OAuth dispatchOAuth
path — new sub formats saml:<NameID> / oidc:<sub>. SCIM v2 deprovisioning included.
crewjam/saml + coreos/go-oidc library choice documented.

## 04:55

CEO oracle re-run: platform moved from "can't show to CISO" to "credible roadmap
conversation". Blockers: messages.db plaintext, SSO not shipped, no HA/support SLA.
Kill shot unchanged: Anthropic managed hosting.

## 05:00

Web docs refreshed (tier-2 web, secrets encryption, SSO roadmap note, K8s howto).
K8s deployment guide written (howto/kubernetes.html). Global /assess skill created.

## 13:00

dashd settings: instructions textarea replaced with dufs workspace links (CLAUDE.md,
PERSONA.md, MEMORY.md, workspace/). dufs compose updated with --allow-edit flag.

## 13:20

max_children added to dashd group settings page (was DB-only). store.SetGroupMaxChildren
reads/patches container_config JSON. Field renders with 0=disabled/-1=unlimited hint.

## 13:40

grants: tier-1 fixed actions expanded — set_group_open, set_observe_window, reset_session,
fork_topic, inject_message, invite_create. Fixed get_routes→list_routes name mismatch (MCP
tool is list_routes; grants had wrong name so tier-1 agents never got routing tools).
auth/policy.go: added set_observe_window case (was hitting default:"unknown tool").

## 14:00

Tools browser: ipc.ListTools(folder, rules) builds MCP server in-memory (zero GatedFns),
returns sorted []mcp.Tool — no separate catalog, auto-updates with ipc.go. GET /v1/tools
REST endpoint (channel-auth). dashd /dash/groups/{folder}/tools renders collapsible
details per tool with description + input schema JSON. "Browse tools →" link in settings.

## 11:00

4 parallel refine agents completed: dashd inline-style→theme-class pass; store sqlPH helper
(drop Repeat/TrimSuffix); root UPPERCASE docs sweep (ARCHITECTURE/SECURITY/ROUTING/CHANGELOG);
web docs sweep (secrets, tier fixes, route tokens shipped). FileProxyHandler restored to
chanlib/handler.go — was deleted in 05c84aa, broke slakd + discd builds.

## 11:51

Slack typing fix: `conversations.typing` via TypingRefresher (3s refresh) for regular
channels/DMs. Pane sessions still use assistant.threads.setStatus. Needs im:write +
channels:write scopes on bot token. All 9 old worktrees merged or accounted for (a71b211
secrets-broker skipped — deferred). v0.42.0 commits staged; images building.

## 13:10

A+C local-user auth shipped: ARIZUKO_LOCAL_SUB env gates MCP tools through
db.Authorize; cli_audit table logs mutating CLI commands with os user + redacted args.
Migration numbered 0061 (0060 conflict with group-model). All tests green.
davd restart loops fixed: dufs --allow-edit→--allow-upload --allow-delete; systemd
overrides used local binary as temp fix while image rebuilt; overrides removed after
new image (12:52) landed. All 3 instances stable (only whapd_krons 401-looping).

## 07:52
Migrate broadcast slimmed: now sends tagline + changelog link only (was full blockquote).
Fix for atlas Slack spam. Agent image rebuilt.

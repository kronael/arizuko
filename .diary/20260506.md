---
summary: |
  arizuko multitenant agent router. Main user: onvos.
  - sloth Discord: discd wired; needs `arizuko run sloth` + `group add discord:<guild>/<ch> <folder>`
  - DM unknown-user reply not yet built (pairing/admission flow for Discord DMs)
  - v0.33.14 tagged; redeploy sloth+marinade still pending
  - CLI bug: --max-uses must precede positional in invite create (unfixed)
  - group portability (group.toml) discussed; not yet specced/built
---

## 00:30

Fixed /invite/ proxyd routing (was falling through to /pub redirect). Committed [fix].
Rebuilt + redeployed sloth. Invite links now work (303 → /auth/login as expected).

Also fixed: group add CLI defaulted folder to raw name (capitalized). Now lowercases.
Coach group had capital-C dir from this bug — merged Coach→coach, fixed DB, rechowned.

## 01:30

User asked about Discord for sloth: OAuth via DISCORD_CLIENT_ID/SECRET, bot via DISCORD_BOT_TOKEN (blank in .env). Same Discord app covers both. No code changes needed — just credentials + restart.

## 13:27

Discord guild mention-only shipped: discd sets verb=mention for @mentions, verb=message for others. Gateway impulse weight 0 for message on Discord guild routes = context-only. `group add discord:*/*` auto-sets impulse_config. Docs updated in discd/README.md + gateway/README.md.

Also fixed: tests used dc: prefix instead of discord: — renamed everywhere. CLAUDE.md updated with make test-e2e and make smoke targets.

## 14:30

Session resumed after context compaction. Delivered 3 talking-point insights to user (ephemeral containers / routing-as-social-primitive / invite-gated subgroup creation). distill subagent produced ant/examples/support/SOUL.md + facts/ skeleton. Products sprint (ant/examples/ templates + --product flag) not yet started.

## 16:00

Products sprint shipped: `--product` flag in cmdCreate resolves ant/examples/<name>/, passes as SetupGroup prototype. Migration 0044 adds groups.product column. 8 product dirs created (personal/support/trip/strategy/pm/reality/creator/socials) with PRODUCT.md manifests and facts/ stubs. No SOUL.md in templates — operator's job. User confirmed this is the right call.

---
summary: |
  v0.32.2 shipped + deployed; Makefile prune fix; CHANGELOG
  blockquote; crackbox pkg/host/ ported (spec 6/12 shipped) +
  10-challenge adversarial review logged. IDLE_TIMEOUT default
  → 60min. specs/5 cleanup: 6-evangelist removed, M-webdav +
  C-message-mcp + Q-unified-routing all flipped to shipped.
  J-sse pending — slink-as-MCP design proposed (per-group
  endpoint, token-bound, 3 tools). Deploy of IDLE_TIMEOUT +
  Q-routing changes pending (`sudo make image` + restart 3).
---

## 00:xx — date rolled

Continuing v0.32.2 work that started 2026-04-30 ~22:00 UTC. Edits
in webd/server.go, webd/slink.go, specs/1/W-slink.md, web docs,
cmd/arizuko/send.go, webd/slink_test.go. Migration 082 added,
MIGRATION_VERSION bumped 81→82. Tests + docs reorg subagent
running in background.

## 04:00 — released v0.32.2

Cut CHANGELOG, tagged v0.32.2, built docker images, redeployed
all 3 instances (krons, sloth, marinade — last two had been on
v0.31.1 since "deploy on user request only" per CLAUDE.md, so
they jumped 0.31.1 → 0.32.2 in one step). Three commits:

- 126c778 [refactor] slink: drop /turn/ URL infix; steer becomes POST /<turn_id>
- 502d4e7 [docs] slink: integrate into concepts/, redirect /pub/slink/

All 3 instances healthy: krons 12/12, sloth 10/10, marinade 10/10.

## 04:00 — `make images` self-foots image-pull failures

User reported 5 chats had errored messages. Investigation:

- 5 monthly cron tasks fired at 04:00:27 UTC
  (`/compact-memories diary|episodes month` to telegram chats for
  happy, krons, mayai, rhias/content, rhias/nemo)
- All 5 spawns failed with exit 125: `pull access denied for
  arizuko-ant, repository does not exist`
- Root cause: `make images` opens with `docker image prune -af`.
  The `-a` flag deletes ANY image not used by a *running*
  container — including our local-only `arizuko-ant:latest` when
  no agent container happens to be active. Build then takes ~2 min
  to recreate it; cron tasks firing during that window try to spawn,
  Docker tries to pull from a registry, fails (we have no registry).
- This is documented as "Nothing works checklist #1" in CLAUDE.md
  but it shouldn't be self-inflicted by our own release process.

Fix: `e36d941 [fix] make images: drop -a from docker image prune`
— now uses `-f` (dangling only).

Cleanup: cleared `errored=1` on the 5 affected message rows so the
next inbound to those chats doesn't replay the failure. Tasks won't
re-fire until next month (cron `0 4 1 * *`); operator can manually
trigger via timed if desired.

## 04:30 — corrected: web/pub is agent-managed per instance, not synced

User pushed back on the "web/pub template-overlay sync gap" I'd
listed as an open follow-up. They're right — `template/web/pub/`
is the SEED for new instance creation. Once an instance exists,
its `/srv/data/arizuko_<inst>/web/pub/` is the agent's workspace
mount (`/workspace/web/pub/`). The agent owns and edits that dir.
There's no "gap" — it's the design.

When upstream docs improve (e.g., today's `concepts/slink.html`
addition), the agent can pull them from `/workspace/self/template/
web/pub/` (the read-only upstream mount) into its own workspace
when/if it wants. No automated rsync needed. The fact that I manually
`cp -r template/web/pub/slink/ /srv/data/arizuko_krons/web/pub/`
during the v0.32.0 ship was an overreach — I shouldn't have written
to the krons agent's workspace from the host. krons agent can
re-organize its docs as it sees fit. Task #22 deleted.

## 04:16 — CHANGELOG blockquote convention (migration 083)

Each release now opens with a `>` blockquote summary so broadcast
tooling can extract just that line. Migration 083 added,
MIGRATION_VERSION 82→83. ant/skills/migrate/SKILL.md updated.

## 04:43 — crackbox specs reorganized

Split into umbrella component: `crackbox/` = thin CLI wrapper +
`egred/` library (egress redirector). Phase 6 specs (9, 10, 11)
rewritten to reflect lib+CLI shape. Sandboxing pulled out of phase 8
(`8/a-crackbox-sandboxing.md` deleted) and into phase 6 as
`6/12-crackbox-sandboxing.md`. New `8/c-sandd.md` defers sandd
(orchestrator daemon) to phase 8. Phase 6 is now the
crackbox-shippable phase; phase 8 is sandd + arizuko integration.

## ~later — adversarial review: crackbox pkg/host/ port

Read all of `crackbox/pkg/host/` (host.go, internal/{manager,
metadata,network,netfilter,qemu,cloudinit,libexec,vm,util}.go +
tests). Generated 10 concrete challenges for the port (spec 6/12):

1. Stop() never sets DeletedAt → netindex + SSH port (99-slot pool)
   permanently consumed; Spawn fails at VM #100.
2. provisionOnce guard checks disk.qcow2 only, not cloud-init.iso.
3. tapScriptPath() silently falls back to bare "crackbox-tap" on
   LibexecDir failure instead of returning an error.
4. iptables chain cbx-<H4> referenced but never created in ported code.
5. discoverVMIP hardcodes /var/lib/misc/dnsmasq-crackbox.leases;
   IPRangeFor() never used to configure bridge/DHCP → dead code.
6. ssh-keygen failure logged but swallowed; Exec silently uses default
   identity.
7. External QEMU kill leaves qemu.pid → detectState() reports ghost VM
   as Running until PID recycled.
8. Exec uses handle.IP (caller), not meta.IP (disk) → stale handle
   targets wrong host.
9. m.mu only used by SetAllowAll; List() + Start()'s meta-IP write are
   racey on meta.yaml.
10. Stop() skips CleanupNetworkRules → iptables rules accumulate for
    stopped VMs; future VM at same IP inherits stale rules.

## ~10:00 — marinade timeout root cause + IDLE_TIMEOUT default fix

User reported "marinade not working". atlas/support container had
been timing out at 1h0m0s repeatedly (4 errored msgs over the day).
Root cause: `IDLE_TIMEOUT=60000` in marinade `.env` parses as 60
SECONDS via `chanlib.EnvDur` (unit is ms, not s). Default is 30 min;
60s killed the container any time the agent went silent during long
think blocks / web fetches. The "Container timed out after 1h0m0s"
error message is misleading — it always reports `cfgTimeout` (the
hard deadline) regardless of which timer fired.

Fix: bumped `core/config.go` IDLE_TIMEOUT default 30min → 60min;
stripped explicit `IDLE_TIMEOUT=` from all 3 instances' `.env` files.
Code change committed (6748614); deploy still pending (`sudo make
image` + restart). Old binary holds old defaults in memory until
restart, so no live regression yet.

## ~10:00 — specs/5 cleanup: 4 specs landed shipped

User asked to verify partials + remove the superseded one.

- `5/6-evangelist`: confirmed redundant (covered by `6/4-hitl-firewall`
  + `6/5-authoring-product`); only inbound ref was `5/index.md`. Deleted.
- `5/M-webdav`: user decision — no Basic Auth, cookie/Bearer is the
  canonical answer; password use case covered by proxyd's local
  OAuth flow which issues a Bearer token. Spec now says shipped.
- `5/C-message-mcp`: shipped via sub. Added `get_thread(jid, limit, before)`
  MCP tool in `ipc/`; extracted shared `store.MessagesByThread` helper
  used by both `MessagesByTopic` (existing) and the new tool. Migration
  084 + MIGRATION_VERSION 83→84.
- `5/Q-unified-routing`: shipped via sub. Three gaps closed: (1)
  dropped `local:` prefix from router (bare folder paths address
  groups directly); (2) added `messages.status` column (migration
  0039 after collision rename) for poll-based outbound — gateway
  now writes bot rows as `pending`, dispatches inline, demotes to
  `sent` on success; the poll loop retries failed/stuck rows; (3)
  unified `send_message` and `send_file` internals via `internalSend`
  helper while keeping them as distinct MCP tools (per project's
  tool-naming rule).

## ~10:10 — cherry-pick reconciliation: TurnID × Status

The Q sub branched from a pre-port HEAD (4220e77) and its worktree
merge would have abandoned all today's earlier commits (crackbox port,
M-webdav, C-message-mcp, IDLE_TIMEOUT). Fix: cherry-picked Q's 5
commits onto fb6a614 (today's tip after C-msg ship). Conflicts:
- `core/types.go`: take both fields (TurnID + Status) on Message struct
- `store/messages.go`: INSERT now writes 19 fields including both
  `turn_id` and `status` columns; scanMessageWithStatus updated to
  scan 20 destinations
- `gateway/gateway.go`: take Q's putAndDeliver poll-based flow but
  thread `turnID` through `dispatchOutbound` → `Channel.Send`
- `ipc/ipc.go`: drop redundant nil-checks but keep `authorizeJID`
- Migration `0038-message-status.sql` collided with existing
  `0038-message-turn-id.sql`; renamed to 0039.

Final HEAD: d78ad68. Build + lint + full test suite all green.

## J-sse open

User wants slink-as-MCP-transport. Recommended shape: per-group
endpoint `POST /slink/<token>/mcp`, token-bound (no JWT layer),
3 tools (`send_message`, `steer`, `get_round`). NOT exposing
`get_history`/`inspect_*` — different threat model from in-container
MCP. Awaiting user confirm before shipping.

## ~later — specs/5 grooming pass continued

Three more specs/5 items resolved in conversation:

- `5/3-agent-messaging`: deleted. User correctly noted it's just
  agent-controlled access into the existing JWT auth surface
  (proxyd issues subjects like `github:alice`, backends verify
  via `auth/middleware.go`). Right answer = drop a Bearer in
  folder secrets; agent uses it on outbound. No new spec needed.
- `5/27-detached-containers`: deleted. `turn_id`/`submit_turn`
  shipped via slink round-handle protocol. Updated W-slink.md
  to drop the dangling cross-reference.
- `5/0-agent-code-modification` and `5/6-extend-gateway-self`:
  moved to phase 8 as `8/d-` and `8/e-`. Self-modification work
  belongs with the longer-arc phase 8 specs (orthogonal-components,
  sandd) rather than the active extension/workflow phase.

Reframes (not deleted):

- `5/H-call-llm-mcp`: narrowed from "general OpenRouter call_llm
  MCP tool" to "oracle skill in `ant/skills/oracle/` that drives
  the codex CLI as a subprocess" — experimental. Folder secret
  carries `OPENAI_API_KEY`. Routing-layer concept deferred until
  a second oracle exists.
- `5/b-memory-skills-standalone`: broadened from "extract memory
  skills" to "ant standalone — Claude Code distribution + sandbox
  spawn via CLI". `ant run` boots agent in dockbox today / crackbox
  later; mirror crackbox's shippable-component shape (own CLI,
  image, docs, README under 300 words).

## J-sse slink-MCP — sub completed but worktree-stale

Slink-as-MCP shipped in agent worktree (4 commits): `injectSlink`
helper extracted from `handleSlinkPost`; new `webd/slink_mcp.go`
mounts `mark3labs/mcp-go` `StreamableHTTPServer` at
`POST /slink/<token>/mcp`; 3 tools (`send_message`, `steer`,
`get_round`); 5 tests pass. BUT — same problem as Q sub: the agent
worktree branched from a pre-port HEAD, used migration `079`
(collides with main's `084`), and didn't see today's CHANGELOG
blockquote convention. Cherry-pick reconciliation pending.

## Open

- Cherry-pick slink-MCP sub onto current HEAD (similar to Q sub
  reconciliation earlier today)
- User decisions still needed: delete `R-ant-go-cli` (fold into
  ant-standalone)? delete `Z-cli-chat` (once ant ships, `ant chat`
  covers it)?
- Deploy IDLE_TIMEOUT + today's stack to all 3 instances

## ~10:30 — slink-MCP cherry-pick reconciliation

Same problem as Q sub: agent worktree branched from old HEAD,
migration `079-slink-mcp.md` collided with `079-crackbox-egress.md`,
CHANGELOG didn't have blockquote convention. Resolved:
- Renumbered migration 079 → 085, fixed in-file header
- Bumped MIGRATION_VERSION 84 → 85
- Merged CHANGELOG entries (kept get_thread + slink-MCP under one
  blockquote summary)
- Reconciled W-slink.md and J-sse.md content across branches
- 4 commits cherry-picked clean: `1d743aa` → `6635622`
- Build + webd tests green

## ~10:45 — specs/5 deep grooming pass

Continued cleanup based on user feedback:

- `5/b-memory-skills-standalone` → renamed to `5/b-ant-standalone`
  and rewritten as "ant agent-as-a-folder". The folder defines
  persona/skills/memory; one command `ant <folder>` with mode
  flags (`--prompt`, `--mcp`, `--sandbox`). arizuko consumes ant
  the same way it consumes crackbox — separate binary, clean
  boundary.
- `5/Z-cli-chat`: deleted. Subsumed by `arizuko chat <inst> [group]`
  routing through `ant <folder>` underneath.
- `5/R-ant-go-cli`: deleted. Folded into b-ant-standalone (one Go
  binary spans CLI + container runtime).
- `5/d-self-improvement`: deleted. Folded into `6/8-self-eval-skill`
  (chose exit-trigger over cron-trigger; one source of truth).
- `5/3-agent-messaging`: deleted earlier — covered by JWT auth +
  folder secrets.
- `5/27-detached-containers`: deleted earlier — `turn_id`/`submit_turn`
  shipped via slink round-handle.
- `5/0-agent-code-modification`, `5/6-extend-gateway-self`: moved
  to phase 8/d and 8/e.
- `5/E-plugins`: deleted. Overengineered governance flow — ant's
  static skills/ folder + operator approval at a higher layer is
  enough.
- `5/c-agent-services`: deleted. "Local cloud" / generic infra,
  not arizuko-shaped.
- `5/e-replaceability-research`: moved to `8/f-replaceability-research`
  (research-only, deferred to phase 8).
- `5/9-identities`: deleted. Standard OAuth account-chooser pattern;
  no spec-worthy design — `auth_users.linked_to_sub` column +
  callback collision branches is the whole story.
- `5/H-call-llm-mcp`: narrowed to "oracle skill" (Claude → codex CLI
  via subprocess + folder secret). Routing-layer concept deferred
  until a second oracle exists.
- `4/R-products` and `6/5-authoring-product`: trimmed to reference
  ant's folder shape (the layout description was duplicated).

specs/5 went from 18 unshipped at session start → 5 unshipped now
(H-oracle, N-listener, S-jid-format in flight, T-voice-synthesis,
b-ant-standalone).

## ~11:00 — auth-landscape moved to root

`5/33-auth-landscape.md` was a reference doc explaining how
`groups`+`user_groups`+`routes` compose into agent grants — not a
design spec for future work. Moved to root as `GRANTS.md` (the
contributor-ring location per CLAUDE.md doc hierarchy). Spawned a
sub to verify every claim against current code.

## ~11:00 — three subs running in parallel

Big subs, well-isolated workloads:
- JID format port (worktree) — spec 5/S-jid-format
- GRANTS.md verify-and-refine (parent tree, doc-only)
- OAuth account linking + collision (worktree) — `auth_users.linked_to_sub`
  column + callback collision branches + `/dash/profile` link button

## ~12:00 — JID sub stopped at commit 1, asked 7 questions

Sub shipped `8d64cae [feat] core: JID, ChatJID, UserJID types` and
halted before the migration. The questions were good — real design
ambiguities the spec didn't cover:

- Discord stored rows lack guild_id (recommended: chats.is_group +
  `discord:_/<channel>` placeholder for guild channels with unknown
  guild)
- Mastodon stored rows lack host (recommended: drop host from format
  → `mastodon:account/<id>`)
- `routed_to` is folder paths, not JIDs — skip migration there (spec
  wording was wrong)
- `reply_to_sender`, `chats.jid`, `user_jids.jid`, `grants.jid`,
  `onboarding.jid`, `agent_cursor` all need migration too
- Typing scope: struct fields only, helper params stay `string`
- whapd / twitd already conform; no-op

Sent answers back to user; awaiting confirm before resuming sub.

## ~13:00 — GRANTS refine sub completed

`8d5aa87 [docs] GRANTS: verify against code, fill gaps post-Q-routing`.
~25 claims verified, several stale — `user_groups` schema column
name was `folder` not `glob`, migration file is 0013 not 0006,
broken xref to `28-acl.md` corrected to `5/29-acl.md`. Material
gaps filled: `grant_rules` overlay (gateway.go:883) was missing
entirely; `auth.Authorize` outbound subtree containment was missing;
`auth.RequireSigned` / `StripUnsigned` HTTP-edge layer; `secrets`
table + folder-scoped resolution + AES-GCM at rest (migration 0034);
env-only mute states (`SEND_DISABLED_CHANNELS` / `_GROUPS`); the
`local:` prefix drop from today; tier-default tables; opening
orientation paragraph + `SECURITY.md`/`ARCHITECTURE.md` cross-refs.

One divergence flagged: `grants/README.md` lists a `NarrowRules`
public API symbol that doesn't exist in `grants/grants.go`. Doc-only
issue; logged for follow-up.

## ~13:00 — OAuth account-linking sub completed

6 commits, 14 files (10 modified, 4 new), 22 tests added, all green:

- `50c7636 [feat] store: add auth_users.linked_to_sub column (migration 0040)`
- `f9a3d6d [feat] auth: resolve linked_to_sub at JWT mint time`
- `6bb51ff [feat] auth: OAuth callback handles 7-branch collision`
- `50a869b [feat] dashd: /dash/profile lists linked accounts + Link buttons`
- `c7271c0 [docs] migration 086 + CHANGELOG: account linking + collision UX`
- `f6ec8af [specs] 1/f-auth-oauth: account linking + 7-branch collision`

7 collision branches mapped 1:1 to plan. Sub correctly identified
TWO resolve points (not one): JWT mint in `auth/web.go::issueSession`
AND proxyd cookie-fallback in `proxyd/main.go::tryAuth`. Refresh-cookie
sessions outlive any link operation, so the cookie path needs
canonicalization too. Downstream code never re-resolves; both call
sites canonicalize once. State-token format extended backwards-
compatibly (3-part legacy or 4-part with `intent=link` payload).

Two-existing-account merge is refused with HTTP 409 — deliberate;
no UX yet for "combine grants of two real users."

`/dash/profile/` page lists linked subs + a Link button per provider
the user hasn't claimed yet, pointing at `/auth/login?intent=link`.

Sub did NOT execute manual smoke; deploy still pending.

## State at end of session

- specs/5 unshipped: 5 (H-oracle, N-listener, S-jid-format paused,
  T-voice-synthesis, b-ant-standalone). Down from 18 at session start.
- JID sub paused at commit 1 (`8d64cae`); 7 design questions answered
  in the user-facing reply, awaiting user sign-off to resume.
- Deploy of today's stack (IDLE_TIMEOUT default, Q-routing, slink-MCP,
  get_thread, GRANTS doc, OAuth linking) still pending: `sudo make
  image` + restart 3 instances.

## ~13:30 — N-listener evaporates into autocalls + small primitives

User pushed back on N-listener as a system-level mode and walked
through the design: agent-controlled, not gateway-controlled. Final
shape after the back-and-forth:

1. Drop N-listener spec entirely (no `groups.mode='listener'`, no
   per-group toggle)
2. Add `unread` + `errors` to the `<autocalls>` block (already
   shipped per spec 5/31). Per-JID counts, computed at prompt-build
   time. Free for the agent to read, no tool call.
3. Add `since` param to `inspect_messages` (~20 LOC) so the agent
   can read only what's new
4. Add `digest/SKILL.md` describing the pattern: check unread in
   autocalls, if non-zero call inspect_messages(since=last_run),
   summarize, send. Operator schedules a cron via `scheduled_tasks`
   for the wake-up

Agent now decides: when the agent runs (for any reason — a trigger
message, a scheduled task, a /digest invocation), it SEES the
accumulated unread counts and decides whether to act. No system
mode.

Same kit covers errors: if `errors > 0` in autocalls the agent can
investigate without a separate diagnostic flow.

## ~13:30 — `groups.state` removal sub launched

Subagent spawned to drop the `groups.state` machinery wholesale:
no `state` column, no closure logic in `timed`, no
`spawn_ttl_days`/`archive_closed_days` config, no soft-delete. User
reasoning: a group is alive if its row exists; if it's not used,
leave it; storage is cheap. The closure-then-archive cycle was
overengineering for a marginal "soft-delete grace period" win.

Sub is in worktree `agent-ad40fb4b...`; will reconcile when it
merges (small expected). I committed the spec trim to 4/26-prototypes
on parent tree as `384a6dc` so the conflict (if any) is on text only.

## State of the world

Subs running: state-removal (worktree). Three subs done today
(GRANTS refine, OAuth linking, slink-MCP cherry-pick); one paused
(JID port at 8d64cae, awaiting user confirm of 7 questions).

specs/5 unshipped: 5, will become 4 once N-listener is deleted
(H-oracle, S-jid-format, T-voice-synthesis, b-ant-standalone).

Open primitives to add (small, deferred until use case):
- `inspect_messages.since` param
- `unread` + `errors` in autocalls
- `digest/` skill recipe
- Audit log of rule/route changes (mentioned by user, no spec yet)

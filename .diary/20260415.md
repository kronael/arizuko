---
summary: |
  28b-onboarding implemented + v0.28.0 released. Main user: onvos.
  - implemented: 28-acl (ACL flip) + 28b-onboarding (web dashboard)
  - security: XSS escaping, token consumption, JID uniqueness
  - cleanup: removed dead approve/reject flow (-380 lines)
  - released: v0.28.0, deployed all 3 instances
  - fixed: IPC duplicate root cause was stale v0.27.0 images (never rebuilt)
---

## 22:16

Released v0.28.0 (3b50257). Rebuilt arizuko + arizuko-ant images.
Deployed all 3 instances. Sloth duplicate message was NOT a new bug —
v0.27.0 images were never rebuilt after the IPC nudge fix (70ae627).
Deployed logs showed old log string "stale IPC messages" vs fixed
"stale progress nudges". Now deployed with correct discardNudges().

## 21:18

28-acl spec rewritten: glob-matched user_groups (*, ** for subtrees),
operator is just a user with ** entry. Output styles fixed: bare URLs
in telegram/discord (7a7f4c9). Diary hook relaxed to 4h. User wants
simpler plumbing for the ACL chain.

## 15:12

Walked user through 28-acl spec. ACL flip already implemented in
store/auth.go (UserGroups). Remaining: route creation guard in
onbod/ipc, operator bootstrap seed. 28b-onboarding is the real work.

## 10:30

Split spec 28 into 28-acl (ACL flip), 28b-onboarding (user identity
+ dashboard), 28c-slink (deferred). Implementation plan walked through
with user. Awaiting go-ahead to start.

## 08:50

Critical regression: blanket IPC drain (49ef0a8) discarded gateway
steers, not just nudges. Confirmed on sloth — msg 3098 lost. Fix:
tag nudges with source:nudge, only discard those (70ae627). Refined
CLAUDE.md via subagent. Released v0.27.0 (c1a4902).

## 07:36

Deployed agent image with migrations 056-059 to all 3 instances.
Injected episode recompact system messages to 7 groups (krons 5,
marinade 1, sloth 1) for buggy Apr 13-14 episodes.

## 07:15

Fixed compact-memories: glob all project dirs (not just -home-node),
date-filter within transcripts, cross-check messages DB. Audit found
8 buggy episodes across instances (multi-day sessions not filtered).

## 07:00

Added web routing table + auth flow to agent CLAUDE.md. Agents had
no knowledge of /dash/, /auth/, OAuth, JWT. Also fixed web skill —
agents were creating /sub/ paths that don't exist in proxyd.

## 06:50

Python 3.14 via uv in Dockerfile, runtimes section in CLAUDE.md
(python/bun/go/rust). Migration 057.

## 06:30

Found duplicate response root cause: nudgeProgress writes to IPC
dir at msg #200, fires during final text gen (no tool calls),
PostToolUse hook can't drain it, checkIpcMessage re-runs query.
Fix: drain stale IPC after query ends. Thresholds raised 200→500.

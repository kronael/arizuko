---
summary: |
  Session: krons telegram + atlas/search Slack threading fixed.
  - whapd re-pair pending (krons, +420735544891)
  - marinade emaid: needs Gmail App Password
  - mayai CLAUDE.md header wrong ("Slack team agent"): fix deferred
---

## 18:30

Session: durable typing fix (d77464aa), rhias.fiu.wtf web consolidation (pub/krons/rhias/ merged → pub/rhias/nemo/), group CLAUDE.md sweep (5 files refreshed from ant/CLAUDE.md). mayai CLAUDE.md header wrongly says "Slack team agent" — routes WhatsApp.

## 18:45

Sloth trading web access: crackbox egress missing .tech/.sh TLDs. Added quantblue.tech + releases.astral.sh to routd.db. arizuko network CLI bug fixed (2dfa5670): opened messages.db not routd.db.

## 19:00

Slack thread stickiness on marinade: already fixed 2026-06-15 (1c60614a + e84620c8). threadHasBotMessage path confirmed working in logs.

## 19:10

krons health check launched — agent investigating.

## 19:15

krons Telegram root cause: `teled_krons` auto-deregistered at 01:12 after Telegram API 502 flipped `connected=false` → `/health` 503 × 3. Adapter recovered but has no re-register loop. JWT auth and channel registration are decoupled in the split topology: inbound still arrived (JWT passes), outbound silently failed (`deliverRow` dropped the error). 3 replies stuck at `status=pending`.

Fixed: 3625947b (chanlib/run.go: 3-min re-register heartbeat) + 8e61521a (routd/turns.go: log deliver failures). Both deployed to krons — telegram + telegram-rhias re-registered immediately.

## 20:10

atlas/search Slack threading bugs (3 found via audit): (1) thread_replies defaults ON for Slack groups (NULL → ChatIsGroup=true), so replies thread under trigger → invisible in main channel. (2) thread_replies=false suppressed threadRoot but not threadID — in-thread triggers still carried tc.Topic as thread_ts, staying stuck in thread. (3) no CLI to configure thread_replies. Fixed 1+2 in 2b2e6062: deliverTurn now zeroes threadID when thread_replies=false. All atlas groups on marinade set thread_replies=0. Deployed marinade.

---
summary: |
  v0.29.0 + v0.29.1 released + deployed. Disk-full incident fixed.
  Main user: onvos.
  - ACL delivery shipped: auth.MatchGroups, grant CLI, second-JID auto-link
  - db_utils rename, /migrate announce fan-out
  - disk was at 97% → SQLite SQLITE_FULL on krons/marinade
  - pruned 7GB dangling images; DB-full errors stopped
  - v0.29.0 tagged, images rebuilt, all 3 instances restarted
  - marinade groups/atlas/support/{tmp,refs} still holds ~18GB of
    agent-owned ds-sam pipeline data — not platform, needs user call
  - v0.29.1: ACL correctness (** only), six-skill tighten from 14-day
    cross-instance audit (776 loops on krons, 22-msg storm on sloth,
    scaffolding leak on marinade)
---

## 18:30

Big simplification sweep: 20 improve agents across 4 rounds in
parallel. Total delta: 151 files, -5486 net LOC. Key touches:

- operator is implicit (nil-sentinel removed across auth/store/proxyd
  /webd/onbod); `auth.MatchGroups` is the shared ACL helper
- ant/src/index.ts -139 LOC; ant/skills/*/SKILL.md -36% prose
- adapters (teled/discd/mastd/bskyd/reditd/whapd) collapsed
- grants.matchGlob gained `stop` func param; diary.ageLabel signature
  trimmed; chanlib/ipc/compose/dashd tidied
- specs pruned (5 superseded files deleted)
- ARCHITECTURE + README + MIGRATION trimmed
- deleted `ant/skills/dispatch/` — resolve subsumes it
- `ant/skills/typescript/TYPESCRIPT_COMMON_REFERENCE.md` removed
  (foreign `@REDACTED.finance` reference, not arizuko)

Commits: 8a454ff, 7be3a74, 04241c7, 1513d1c, b6afaa3, d0f853e, 5c6ad89.
All tests green. Not yet tagged or redeployed.

User reports atlas group didn't post anything after migration —
logged to bugs.md for investigation.

## 16:00

v0.29.1 deployed to all three. Services active, gated logs clean on
arizuko-ant:v0.29.1: sloth 2/3, krons 7/11, marinade 5/6. User
corrections now authoritative in episodes; classification scaffolding
stays in `<think>`.

## 15:30

Six-skill tighten (resolve, compact-memories, recall-memories, migrate,
ant/CLAUDE.md, migrations/061). Core principle: user corrections
authoritative, agent conclusions not. Conclusions redrawn each recall.
Migrations bumped 60 → 61. Commits: dfe5359 [ant], 93bd652 [refined],
8c9391a [release] v0.29.1.

## 15:00

14-day cross-instance audit (sloth/krons/marinade) via 3 parallel
subagents. Found: (1) SEV-1 `local:*` self-ingestion loop on all 3
instances (776 echoes on krons, 734 on marinade, 22-msg storm on
sloth); (2) system_messages lacks dedup — migration events fire 9×
per day; (3) container OOM (exit 137) 13× in 14 days across
instances; (4) skill scaffolding leaking to users. Logged as priority
fix order; addressed skill-level issues in v0.29.1, loop + OOM need
router/runtime code changes.

## 14:30

ACL correctness bug: `store.UserGroups` treated `*` as operator
instead of `**`. Fixed in c4b4b88 along with onbod.userGroups
alignment, dead-helper cleanup, and auth test trim. All tests green.

## 14:00

Rolled v0.29.0 to all three. Services `active`, gated logs show
"state loaded" + "channel connected" + "arizuko running" on new
`arizuko-ant:latest` image. Groups: sloth 2/routes 3, krons 7/11,
marinade 5/6. MCP sockets present for active groups.

## 13:15

Built + tagged v0.29.0 images (arizuko, arizuko-ant, arizuko-whatsapp,
arizuko-vite). Committed ACL delivery (6d7bea0, +698/-27 across 17
files), release bump (c5c94db), tag v0.29.0.

## 13:00

Disk-full root cause for "database or disk is full (13)" errors on
krons + marinade: 97% use on /. `docker image prune -af` reclaimed
6.97GB. Errors stopped immediately. Marinade `groups/atlas/support/`
holds 18GB of agent-created pipeline data (ds-sam-pipeline 8.5G +
delegation-strategy-pipeline 2G + tmp/ds-sam-pipeline-953 6.2G) —
left alone, agent-owned work; not safe to prune autonomously.

## 00:30

Removed duplicate error notification from queue (077518e) — gateway's
"Failed: ..." path is now the single surface. Rebuilt + redeployed all
three instances. User feedback: reply with episode summaries, not
system-message bullets; details go to diary.
